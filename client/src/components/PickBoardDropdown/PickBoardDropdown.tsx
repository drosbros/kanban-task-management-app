'use client'

import { Menu } from '@headlessui/react'

type Props = {}

const Dropdown = ({}: Props) => {
  return (
    <Menu>
      <Menu.Button>Platform Launch</Menu.Button>
    </Menu>
  )
}

export default Dropdown
